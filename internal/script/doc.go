// Package script loads miaospeed-scripts build artifacts: a single .js
// file or a directory containing an index.json manifest. It can also load
// a miaospeed-scripts build embedded into the binary at build time; see
// EmbeddedAvailable, EmbeddedVersion, and LoadEmbedded.
package script
